# 公式ベースイメージに torch 2.5.1 + torchvision が同梱（CUDA 12.4）。追加分のみ記載。
orena-focus>=0.3.4
transformers>=5.14
accelerate>=1.14
peft>=0.19
bitsandbytes>=0.49
qwen-vl-utils>=0.0.14
numpy>=1.23.0
pillow>=9.0
# ⚠️`flash-linear-attention` はここに書かない。最新版は **Triton 3.1（ベースイメージ同梱）と
#   非互換**（"Autotuner.__init__() got an unexpected keyword argument 'do_bench'"）。
#   Triton を上げると torch 2.5.1 を壊しかねないので、Dockerfile で
#   **版を固定し失敗許容**で入れる。無ければ素の PyTorch 実装で動く（遅いが正しい）。
# ⚠️`causal-conv1d` は入れない。**ソースビルドを要求し nvcc が無いイメージでは必ず失敗**する
#   （2026-09-01: "nvcc was not found" → "NameError: bare_metal_version"）。
#   fla の補助でしかなく、無くても動く。
# ★フレーム取得。公式テンプレートが推奨する一括読み（get_batch）。
#   ⚠️import は必ず torch の後（先に読むと CUDA 初期化が壊れる既知の問題）。
decord>=0.6.0
# ⚠️TensorRT はここに書かない。`tensorrt-cu12` もその実体（-libs / -bindings）も
#   PyPI に wheel を持たず（NVIDIA の独自インデックス経由）、`--only-binary` で必ず失敗する。
#   Dockerfile で **別ステップ・失敗許容**として入れる（無ければ PyTorch にフォールバックする）。
# ★工程分類モデル（ConvNeXtV2 + MS-TCN）を読むのに要る。
#   resources/phase_lib/{model,evaluate,train_tcn,dataset}.py が import する。
#   入れ忘れると「工程モデルを読めない」で**黙って工程なしに落ちる**（−0.0081 相当）。
timm>=1.0
pyyaml>=6.0
scikit-learn>=1.3
pandas>=2.0
